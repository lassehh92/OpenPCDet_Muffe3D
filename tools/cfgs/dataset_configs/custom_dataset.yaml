DATASET: 'CustomDataset'
DATA_PATH: '../data/custom'

POINT_CLOUD_RANGE: [-4, -4, -2, 4, 4, 2]

CLASS_NAMES: ['Valve', 'Tap_connection', 'Joint']  

MAP_CLASS_TO_KITTI: {
    'Valve': 'Car',
    'Tap_connection': 'Pedestrian',
    'Joint': 'Cyclist'
}

DATA_SPLIT: {
    'train': train,
    'test': val
}

INFO_PATH: {
    'train': [custom_infos_train.pkl],
    'test': [custom_infos_val.pkl],
}

POINT_FEATURE_ENCODING: {
    encoding_type: absolute_coordinates_encoding,
    #used_feature_list: ['x', 'y', 'z'],
    #src_feature_list: ['x', 'y', 'z'],
    used_feature_list: ['x', 'y', 'z', 'red', 'green', 'blue'],
    src_feature_list: ['x', 'y', 'z', 'red', 'green', 'blue'],
}

DATA_AUGMENTOR:
    DISABLE_AUG_LIST: ['placeholder']
    AUG_CONFIG_LIST:
        - NAME: random_world_flip
          ALONG_AXIS_LIST: ['x', 'y']

        - NAME: random_world_rotation
          WORLD_ROT_ANGLE: [-0.78539816, 0.78539816]

        - NAME: random_world_scaling
          WORLD_SCALE_RANGE: [0.95, 1.05]

DATA_PROCESSOR:
    - NAME: mask_points_and_boxes_outside_range
      REMOVE_OUTSIDE_BOXES: True

    - NAME: shuffle_points
      SHUFFLE_ENABLED: {
        'train': True,
        'test': False
      }

    - NAME: transform_points_to_voxels
      #VOXEL_SIZE: [0.03125, 0.03125, 0.1]
      #VOXEL_SIZE: [0.03125, 0.03125, 0.03125]
      VOXEL_SIZE: [0.02, 0.02, 0.02]
      MAX_POINTS_PER_VOXEL: 5
      MAX_NUMBER_OF_VOXELS: {
        'train': 40000,
        'test': 40000
      }
